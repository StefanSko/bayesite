# Changelog

All notable user-visible changes to Bayesite are recorded here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Bayesite uses semantic versioning while its public formats remain explicitly
versioned on their own.

## [Unreleased]

### Added

- Expand SBC calibration with `eight_schools_non_centered` for non-centered
  hierarchical geometry, `varying_intercepts_poisson` for a hierarchical
  Poisson GLM, `linear_regression_n64` for n=64 regression, and `mvn_cholesky`
  for an MVN with a parameter-scaled Cholesky factor.
- Add `centered_eight_schools` as a must-reject SBC negative control that
  asserts the rank-uniformity gate detects the centered funnel's known
  miscalibration.
- Add a deterministic conformance summary report job that combines stage
  results and links the per-gate reports found in CI artifacts.

### Changed

- Rename the G6, G7, and G11 conformance CI gates to descriptive cross-engine,
  cross-backend, and SBC calibration names while retaining ladder-rung
  references in validation documentation.

## [0.2.2] - 2026-07-14

### Added

- Add a mandatory, stdlib-only SBC rank-uniformity conformance gate using
  Monte Carlo-calibrated simultaneous ECDF confidence bands.
- Add self-contained HTML report options for the G6 and G11 statistical gates
  and upload their reports as conformance CI artifacts.
- Add an optional `sample.thin` setting to `bayesite sbc` scenarios and
  `settings.thin` to the protocol SBC command: ranks use every thin-th
  retained draw per chain while R-hat/ESS diagnostics keep the full series,
  and reports state `thin` and the thinned rank support ([#36]).

### Changed

- Make G11 SBC ranks use ESS-adaptive thinning so its iid ECDF calibration no
  longer falsely rejects autocorrelated NUTS draws, and guard bias diagnoses
  against centered envelope exits ([#36]).
- Replicate the G6 nuts-rs statistical oracle across deterministic seeds and
  add signed Stouffer aggregation to detect small systematic sampler biases.

## [0.2.1] - 2026-07-10

### Added

- Decode, bind, transform, evaluate, and forward-simulate Bayeswire
  `VectorBounds` constraints for censored and partially observed vectors.
- Validate the final Bayeswire corpus, including closed namespaced `Submodel`
  declarations flattened to ordinary dotted IR names.

### Changed

- Resolve finite base-distribution support for `VectorBounds` from the unique
  same-name direct or scatter owner, independent of stochastic-site order.
- Classify declaration-backed sites before prior-predictive drawing and reject
  additional density Factors that have no supported ancestral semantics.
- Refresh the vendored Bayeswire specification and corpus to merged bayescycle
  commit `e9784f5`.

### Fixed

- Preserve stable bounded draws in extreme distribution tails.
- Align vector support through wrapped negative missing indexes and validate
  missing-value shape against `missing_idx` before drawing.
- Defer support folding when a required support value is generated by an
  earlier stochastic site.

## [0.2.0] - 2026-07-06

### Added

- Support the canonical wrapped data document and neutral model/data
  fingerprint contract.
- Add the machine-readable `capabilities` command.
- Implement explicit `Truncated` distributions and stable far-tail simulation.
- Add merge-blocking validation, cross-backend conformance, and a pinned
  nuts-rs statistical oracle.

### Changed

- Adopt the neutral `bayeswire_ir` envelope and vendored Bayeswire
  specification/corpus snapshots.

## [0.1.0] - 2026-06-24

### Added

- Initial zero-dependency Bayesite CLI and Rust runtime for decoding Bayeswire
  IR, evaluating log densities, running NUTS, diagnostics, predictive
  workflows, recovery, and SBC.
- Reproducible release archives and checksums for Linux, macOS, and Windows.

[#36]: https://github.com/StefanSko/bayesite/issues/36
[Unreleased]: https://github.com/StefanSko/bayesite/compare/v0.2.2...HEAD
[0.2.2]: https://github.com/StefanSko/bayesite/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/StefanSko/bayesite/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/StefanSko/bayesite/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/StefanSko/bayesite/releases/tag/v0.1.0
